# Meeting Assistant

Утилита для записи, транскрибации и суммаризации встреч (созвонов) на macOS с поддержкой двух движков записи: нативного (ScreenCaptureKit) и legacy (sounddevice).

## 🎯 Возможности

- **Два режима записи аудио**:
  - **Legacy Mode**: Захват через Aggregate Device (BlackHole + Mic) с помощью sounddevice
  - **Native Mode**: Нативный захват macOS через ScreenCaptureKit и AVFoundation (требует macOS 13.0+)
- **Транскрибация**: Deepgram Nova-2 с поддержкой русского языка
- **Диаризация**: Автоматическое разделение спикеров с временными метками `[MM:SS] Speaker N:`
- **Суммаризация**: Генерация итогов встречи с помощью Google Gemini 2.0 Flash
- **Web API**: Интерактивный интерфейс OpenAPI/Swagger для загрузки файлов
- **Retry Logic**: Автоматические повторные попытки с экспоненциальной задержкой при сбоях API
- **Graceful Shutdown**: Корректная остановка записи по Ctrl+C
- **Два формата вывода**: 
  - `transcript_YYYYMMDD_HHMMSS.md` - полный лог с временными метками
  - `summary_YYYYMMDD_HHMMSS.md` - краткие итоги с датой встречи, темами, решениями и задачами

## 📋 Требования

- **macOS** (проверено на Apple Silicon)
- **Python 3.12+** (рекомендуется 3.12 для лучшей совместимости)
- **Для Legacy Mode**: BlackHole или аналогичный аудио-драйвер
- **Для Native Mode**: macOS 13.0+ (Ventura) с разрешениями Screen Recording и Microphone
- **API Ключи**:
  - [Deepgram API Key](https://console.deepgram.com/)
  - [Gemini API Key](https://aistudio.google.com/)

## 🚀 Установка

### 1. Клонируйте репозиторий

```bash
git clone <repository-url>
cd meeting-assistant
```

### 2. Создайте виртуальное окружение

```bash
python3.12 -m venv venv
source venv/bin/activate
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

**Примечание**: Установка PyObjC frameworks может занять несколько минут.

### 4. Настройте API ключи

Вы можете использовать любой из двух методов:

#### Метод 1: Файл `.env` (рекомендуется)

```bash
cp .env.example .env
```

Отредактируйте `.env`:
```env
DEEPGRAM_API_KEY=ваш_ключ_deepgram
GEMINI_API_KEY=ваш_ключ_gemini
```

#### Метод 2: Файл `config.json`

Отредактируйте `config.json` и добавьте ключи:
```json
{
  "api_keys": {
    "deepgram": "ваш_ключ_deepgram",
    "gemini": "ваш_ключ_gemini"
  }
}
```

**Приоритет**: Ключи из `.env` имеют приоритет над `config.json`.

## ⚙️ Настройка аудио

### Legacy Mode (sounddevice + BlackHole)

1. Установите [BlackHole 2ch](https://github.com/ExistentialAudio/BlackHole)
2. Откройте **Настройка Audio-MIDI** (Audio MIDI Setup)
3. Создайте **Агрегатное устройство** (Aggregate Device) с именем `Unit`
4. Включите в него:
   - Ваш микрофон
   - BlackHole 2ch
5. Убедитесь, что BlackHole выбран как устройство вывода в macOS или в приложении для созвонов

### Native Mode (ScreenCaptureKit)

> **⚠️ Важно**: Native Mode - это экспериментальная функция с placeholder-реализацией. 
> Для производственного использования рекомендуется Legacy Mode.

1. Требуется macOS 13.0+ (Ventura)
2. При первом запуске система запросит разрешения:
   - Screen Recording
   - Microphone Access
3. Измените в `config.json`:
   ```json
   {
     "recording_method": "native"
   }
   ```

## 📖 Использование

### CLI: Запись новой встречи

Запустите скрипт для начала записи. Нажмите **Ctrl+C** для остановки.

```bash
python main.py
```

Вывод:
```
Meeting Assistant initialized.
============================================================
✓ Configuration loaded
  Recording method: legacy
✓ API keys validated
✓ Recorder initialized: legacy
✓ Transcription processor initialized
✓ Summarizer initialized
✓ Session directory: output/2026_02_06 12:15
============================================================

Recording to output/2026_02_06 12:15/recording.wav
Using device: Unit (index 4)
Press Ctrl+C to stop recording.
```

### CLI: Обработка существующего файла

```bash
python main.py -f /путь/к/файлу.wav
```

### Web API (Swagger UI)

Запустите сервер:

```bash
python api.py
```

Откройте в браузере: [http://127.0.0.1:8000/docs](http://127.0.0.1:8000/docs)

Через Swagger UI можно:
- Загрузить аудиофайл
- Получить транскрипт
- Получить саммари

## 🏗️ Архитектура

### Структура проекта

```
meeting-assistant/
├── core/
│   ├── config_manager.py      # Управление конфигурацией
│   ├── processor.py            # Deepgram интеграция + retry logic
│   ├── summarizer.py           # Gemini интеграция + retry logic
│   └── recorders/
│       ├── __init__.py
│       ├── base_recorder.py   # Абстрактный базовый класс
│       ├── legacy_recorder.py # sounddevice реализация
│       └── native_recorder.py # ScreenCaptureKit реализация
├── recorder_factory.py         # Factory Pattern для создания рекордеров
├── main.py                     # CLI точка входа
├── api.py                      # FastAPI сервер
├── config.json                 # Конфигурация приложения
├── .env                        # API ключи (не в git)
├── requirements.txt
└── output/                     # Результаты по датам/времени
```

### Factory Pattern

Приложение использует Factory Pattern для выбора между recording engines:

```python
from core.config_manager import ConfigManager
from recorder_factory import RecorderFactory

config = ConfigManager()
recorder = RecorderFactory.create_recorder(config)
```

Factory автоматически:
- Проверяет совместимость системы
- Создает нужный recorder
- Применяет настройки из конфигурации
- Обрабатывает fallback на legacy mode при ошибках

### Retry Mechanism

Все API вызовы (Deepgram, Gemini) используют retry logic с экспоненциальной задержкой:
- 3 попытки максимум
- Задержки: 1s, 2s, 4s
- Автоопределение retryable ошибок (network, timeout, DNS)

### GRPC DNS Fix

Приложение автоматически устанавливает `GRPC_DNS_RESOLVER=native` для предотвращения DNS resolution ошибок на macOS.

## 🔧 Конфигурация

### config.json

```json
{
  "recording_method": "legacy",  // "legacy" или "native"
  "api_keys": {
    "deepgram": "",              // Опционально (приоритет у .env)
    "gemini": ""                 // Опционально (приоритет у .env)
  },
  "legacy_settings": {
    "device_name": "Unit",       // Имя Aggregate Device
    "samplerate": 48000,         // Частота дискретизации
    "channels": null             // null = автоопределение
  },
  "native_settings": {
    "samplerate": 48000,
    "exclude_current_process": true
  },
  "transcription": {
    "model": "nova-2",           // Deepgram модель
    "language": "ru",            // Язык транскрибации
    "diarize": true,             // Диаризация спикеров
    "smart_format": true,        // Умное форматирование
    "timeout": 600               // Таймаут в секундах
  }
}
```

## 📝 Формат вывода

### Transcript (transcript_YYYYMMDD_HHMMSS.md)

```markdown
# Meeting Transcript

**Date/Time:** 2026-02-06 12:15:30

---

[00:00] Speaker 0: Привет всем! Начинаем нашу встречу.

[00:05] Speaker 1: Здравствуйте! Я готов.

[00:12] Speaker 0: Отлично. Давайте обсудим новый проект...
```

### Summary (summary_YYYYMMDD_HHMMSS.md)

```markdown
# Meeting Summary

## Дата и время встречи
2026-02-06 12:15:30

## Ключевые темы
- Обсуждение нового проекта
- Планирование спринта
- Распределение задач

## Решения
- Начать разработку с MVP
- Еженедельные синхронизации по понедельникам

## Задачи
- [ ] Подготовить дизайн макеты - Спикер 0
- [ ] Настроить CI/CD pipeline - Спикер 1
- [ ] Написать документацию - Спикер 0
```

## 🐛 Устранение неисправностей

### GRPC DNS Resolution Error

Если видите ошибки типа `DNS resolution failed`:
```bash
# Приложение автоматически устанавливает, но можно вручную:
export GRPC_DNS_RESOLVER=native
python main.py
```

### API Key Errors

```
❌ LLM Error: 400 API key not valid
```

**Решение**: Проверьте API ключи в `.env` или `config.json`:
```bash
# Проверить .env
cat .env

# Проверить config.json
cat config.json
```

### Empty Audio File

```
❌ Error: Audio file is empty or was not created correctly.
```

**Возможные причины**:
1. Микрофон не выбран в системе
2. Aggregate Device не настроен правильно
3. Нет разрешений на доступ к микрофону

**Решение**:
- Проверьте System Preferences → Security & Privacy → Microphone
- Убедитесь, что Aggregate Device создан правильно
- Проверьте, что в Aggregate Device включены нужные устройства

### Native Recorder Not Available

```
Native recorder not available. Please install required dependencies
```

**Решение**:
```bash
pip install pyobjc-framework-ScreenCaptureKit pyobjc-framework-AVFoundation pyobjc-framework-CoreAudio
```

Если установка не помогает, используйте Legacy Mode:
```json
{
  "recording_method": "legacy"
}
```

## 🔬 Особенности реализации

- **Diarization**: `diarize=true` в Deepgram для точного определения спикеров
- **Smart Format**: Автоматическая пунктуация и форматирование чисел
- **Error Handling**: Корректная обработка прерывания (Ctrl+C) для предотвращения повреждения файлов
- **Signal Handling**: Graceful shutdown с вызовом `recorder.stop()`
- **Timestamped Files**: Все выходные файлы содержат timestamp для уникальности
- **Meeting Metadata**: Дата и время встречи автоматически добавляются в транскрипт и саммари

## 🤝 Вклад

Приветствуются Pull Requests! Особенно для:
- Полной реализации Native Recorder с ScreenCaptureKit
- Поддержки других языков
- Улучшения retry logic
- Дополнительных форматов экспорта

## 📄 Лицензия

MIT License

## 🙏 Благодарности

- [Deepgram](https://deepgram.com/) - за отличный API транскрибации
- [Google Gemini](https://ai.google.dev/) - за мощный LLM
- [BlackHole](https://github.com/ExistentialAudio/BlackHole) - за виртуальный аудио-драйвер
